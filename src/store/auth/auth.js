import {
  firebase,
  googleAuthProvider
} from '../../firebase/firebase';

const state = {
  uid: null
}

/* const mutation = {
  login: () => {
    return [firebase.auth().signInWithPopup(googleAuthProvider)；
  }
} */
